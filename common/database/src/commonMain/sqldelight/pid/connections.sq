import cz.lastaapps.core.domain.model.StopName;




CREATE TABLE connections (
    id INTEGER NOT NULL PRIMARY KEY,
    fromStation TEXT AS StopName NOT NULL,
    toStation TEXT AS StopName NOT NULL
);

insertConnection:
    INSERT INTO connections VALUES (:id, :from, :to);

getAllConnections:
    SELECT * FROM connections ORDER BY id;
